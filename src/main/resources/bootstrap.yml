
## 배포

#spring:
#  cloud:
#    config:
#      uri: https://config.apps.sys.paas-ta-dev10.kr
#      name: apigateway # yml 파일명 써주면 된다.
#  profiles:
#    active: prod
# 로컬
spring:
  cloud:
    config:
      uri: http://localhost:8888
      name: apigateway # yml 파일명 써주면 된다.
  profiles:
    active: dev

